;; extends

(operation_definition
  (name) @operation.definition)

(fragment_name
  (name) @fragment.name)

(object_value
  (object_field
    (name) @object.field))

(field
  (alias
    (name) @field.alias))
